Name=DMD Binary Region of Interest Mode
RequiredDevices=DMD_4500_DEVICE_TYPE
Categories=DMD,Binary,ROI
Description=Region of interest (ROI) example where a DMD displays a binary (1-bit) all on (white) image followed by an all off (black) image. An ROI of 16 DMD columns wide is used to run the DMD at binary frame rates of over 100 kHz.
CreateProjectProgram=python/dmd_binary_roi_example.py
